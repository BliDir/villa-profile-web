import { describe, it, expect } from 'vitest';

// Placeholder — real tests will be added as features are implemented.
describe('villa-profile-web', () => {
  it('passes', () => {
    expect(true).toBe(true);
  });
});
